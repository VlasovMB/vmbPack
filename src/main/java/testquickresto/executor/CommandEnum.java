package testquickresto.executor;

/**
 * The enum Command enum.
 */
public enum CommandEnum {
    /**
     * Pack command enum.
     */
    PACK,
    /**
     * Unpack command enum.
     */
    UNPACK,
    /**
     * Exit command enum.
     */
    EXIT
}
